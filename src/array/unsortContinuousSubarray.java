package array;

public class unsortContinuousSubarray {
	 public int findUnsortedSubarray(int[] nums) {
	     /* (1) Sort the array and compare it to original array.
		  (2) Use two for-loop to compare, first loop check from left and find 
		  first different one and obtain index, second loop check from right and 
		  get index for first different position.
		  (3) The length is the difference of two index.
		  */
		 return 0;
	    }
}
